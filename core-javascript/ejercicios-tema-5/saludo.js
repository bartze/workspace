// 2. Define una función que al clicar el botón del ratón la llames y te escriba
// en la página un saludo
const saludar = () => {
  const saludo = 'This is the way';
  document.getElementById('resultado').innerHTML = saludo;
};
