{
	"name": "knex",
	"version": "1.0.0",
	"description": "My project with knex and express",
	"main": "app.js",
	"scripts": {
		"start": "node --watch app.js",
		"start-dev": "node --watch app.js",
		"start-services": "docker-compose -f docker-compose.yml up -d --build --force-recreate && exit 0",
		"stop-services": "docker-compose -f docker-compose.yml down",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"keywords": [
		"app",
		"express",
		"knex",
		"pg"
	],
	"author": "Iñaki Barceló",
	"license": "ISC",
	"dependencies": {
		"express": "^4.18.2",
		"express-validator": "^6.15.0",
		"knex": "^2.5.1",
		"pg": "^8.13.1"
	}
}
